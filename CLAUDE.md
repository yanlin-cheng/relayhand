# relay —— 会话接力命令仓库

## 项目概述

跨 AI 产品的会话交接命令集合：把长对话压缩成面向"下一棒"任务的接力文档 + 可复制提示词，新对话粘贴即续。本仓库是 relay 的唯一源头，面向不同 AI 产品的用户分发。

## 技术栈

纯提示词资产（markdown），无代码。运行环境：任何支持自定义命令或可粘贴提示词的 AI 对话产品。

## 项目结构

```
relay/
├── README.md            # 门面：是什么、快速开始、用法
├── DESIGN.md            # 设计依据与决策日志
├── claude-code/relay.md # Claude Code 版（$ARGUMENTS、jsonl 正本指针）
├── universal/relay.md   # 通用核心提示词（无命令功能的产品手动粘贴）
└── adapters/            # 各产品适配说明（核心引用 universal/，不复制）
```

## 开发规范

- 通用核心只改 `universal/relay.md`；`adapters/` 只写挂载方式，引用不复制，避免多份漂移
- `claude-code/relay.md` 允许用 Claude Code 专属机制，但不得含任何个人路径、个人知识库配置
- 模板或纪律的任何改动：先在 DESIGN.md 决策日志记一笔（为什么改），再改文件
- 本机实战副本在 `C:\Users\cyl\.claude\commands\relay.md`（含知识库增强层），仓库改动后手动同步过去
- 模板改动必须实测验证：找一场真实长对话跑 /relay，检查产出质量再定稿
- 提交信息用中文，说清"改了什么、为什么"

## 当前阶段

初版建成（2026-09-19），待实战验证：模板总结质量、任务过滤效果、各平台适配说明准确性。
